pipeline{
    
	
    stages{
        stage('Build'){
            agent none
            options{
                skipDefaultCheckout()
		timeout(time:1 , unit:'MINUTE')
            }
            steps{
                echo "Hello world"
		echo "I have committed the code"
            }
        }
    }
}