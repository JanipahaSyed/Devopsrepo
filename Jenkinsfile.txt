pipeline{
    
	
    stages{
        stage('Build'){
            agent none
            options{
timeout(time:1 , unit:'MINUTE')
                skipDefaultCheckout()
		timeout(time:1 , unit:'MINUTE')
            }
            steps{
                echo "Hello world"
		echo "I have committed the code"
            }
        }
    }
}